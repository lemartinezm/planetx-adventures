<div class="visit-info-background">
  <div class="discover-next-container">
    <p class="discover-next-text">Why We're Special?</p>

    <a
      href="#visit-info"
      class="discover-next-button"
      aria-label="View visit info"
    >
      <svg
        class="discover-next-button-arrow"
        xmlns="http://www.w3.org/2000/svg"
        width="20"
        height="20"
        viewBox="0 0 256 256"
        color="currentColor"
        ><path
          fill="currentColor"
          d="m205.66 149.66l-72 72a8 8 0 0 1-11.32 0l-72-72a8 8 0 0 1 11.32-11.32L120 196.69V40a8 8 0 0 1 16 0v156.69l58.34-58.35a8 8 0 0 1 11.32 11.32Z"
        ></path></svg
      >
      <!-- dashed border -->
      <svg
        class="discover-next-button-border"
        width="96"
        height="96"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          d="M48 48 m-42,0 a42,42 0 1,0 84,0 a42,42 0 1,0 -84,0"
          fill="transparent"
          stroke="currentColor"
          stroke-width="1"
          stroke-dasharray="10,10"></path>
      </svg>
    </a>
  </div>

  <div class="visit-info-container" id="visit-info">
    <p class="visit-info-description">
      Every journey you undertake becomes a chapter in the ongoing saga of
      protection, securing the enchantment of our interstellar camping
      destinations for future cosmic explorers.
    </p>
    <div class="visit-info-data">
      <div class="visit-info-data-container">
        <p class="visit-info-data-principal">836</p>
        <p class="visit-info-data-description">Daily guided visits</p>
      </div>
      <div class="visit-info-data-container">
        <p class="visit-info-data-principal">98<span>%</span></p>
        <p class="visit-info-data-description">Visitor satisfied</p>
      </div>
      <div class="visit-info-data-container">
        <p class="visit-info-data-principal">70<span>+</span></p>
        <p class="visit-info-data-description">Biodiversity species</p>
      </div>
      <div class="visit-info-data-container">
        <p class="visit-info-data-principal">158<span>K</span></p>
        <p class="visit-info-data-description">Community followers</p>
      </div>
    </div>
  </div>
</div>

<style>
  .visit-info-background {
    background-image: url("../../assets/visit-image-2-up.webp");
    background-size: cover;
    min-height: 100vh;
    background-position: bottom 20% right 40%;
  }

  .discover-next-container {
    background: linear-gradient(
      to bottom,
      var(--brown) 0%,
      rgba(40, 37, 40, 0.75) 50%,
      rgba(40, 37, 40, 0) 100%
    );
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    padding: 4rem 0 2rem;
  }

  .discover-next-text {
    color: var(--white);
  }

  .discover-next-button {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    border-radius: 50%;
    height: 6rem;
    width: 6rem;
    color: var(--white);
    opacity: 0.5;
    transition: opacity 0.3s;
    cursor: pointer;
  }

  .discover-next-button:hover {
    opacity: 1;
  }

  .discover-next-button:hover .discover-next-button-arrow {
    animation-play-state: paused;
  }

  .discover-next-button-arrow {
    animation-name: bounce;
    animation-duration: 0.75s;
    animation-direction: alternate;
    animation-iteration-count: infinite;
    animation-timing-function: ease;
  }

  .discover-next-button-border {
    position: absolute;
    top: 0;
    left: 0;
  }

  .visit-info-container {
    max-width: 25rem;
    padding: 4rem 1rem;
    margin: 0 auto;
    color: var(--white);
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: flex-start;
    gap: 3rem;
  }

  .visit-info-description {
    text-wrap: balance;
    max-width: 30rem;
  }

  .visit-info-data {
    width: fit-content;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    row-gap: 0.5rem;
    column-gap: 1rem;
  }

  .visit-info-data-container {
    max-width: 8rem;
  }

  .visit-info-data-principal {
    font-size: 2rem;
  }

  .visit-info-data-description {
    text-transform: uppercase;
  }

  @media screen and (min-width: 1024px) {
    .visit-info-container {
      max-width: 1280px;
      padding: 5rem 8rem;
    }

    .discover-next-container {
      grid-column-start: 1;
      grid-column-end: 3;
    }

    .visit-info-data {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  @keyframes bounce {
    from {
      transform: translateY(0);
    }
    to {
      transform: translateY(-0.25rem);
    }
  }
</style>
