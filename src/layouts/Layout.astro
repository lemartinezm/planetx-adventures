---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Embark on an interstellar odyssey with PlanetX Adventures! Your adventure begins here as you explore special camping spots in the cosmic frontiers, uncovering the mysteries of uncharted terrains. Whether you're a seasoned spacefarer or new to off-world camping, our expert guides will navigate you through cosmic revelation, imparting wisdom on extraterrestrial landscapes, celestial phenomena, and interplanetary survival techniques. Ignite your sense of wonder, cultivate new connections, and craft enduring memories amid the infinite wonders of the cosmos. Join us for a unique intergalactic camping experience like no other!"
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  :root,
  ::backdrop {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
    --white: #ffffff;
    --black: #0a0d15;
    --black-75: #47494f;
    --black-50: #84868a;
    --black-25: #c2c2c4;
    --black-10: #e7e7e8;
    --black-5: #f3f3f3;
    --light-blue: #91c2ef;
    --dark-blue: #0b0e16;
    --dark-blue-transparent: #0b0e16e5;
    --brown: #282528;
    --dark-brown: #1b171a;
    --green: #619c24;
    --error: #9b2c2c;
    --success: #2f855a;
  }
  html {
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
    background-size: 224px;
    scroll-behavior: smooth;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }

  a {
    text-decoration: none;
    color: currentColor;
  }

  a:visited {
    color: currentColor;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  li {
    list-style-type: none;
  }
</style>
